name: Enviar Palabra del Día

on:
  # 1) Sigue corriendo cada día a las 15:00 UTC (→ 12:00 Córdoba)
  schedule:
    - cron: '0 15 * * *'
  # 2) Y ahora también podrás iniciarlo MANUALMENTE
  workflow_dispatch:

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - name: Notificar devocional diario
        run: |
          curl -X POST https://fundacionmisionvida7.vercel.app/api/send-notification \
            -H "Content-Type: application/json" \
            -d '{
              "title": "📖 Palabra del Día",
              "body":  "¡Tu devocional de hoy ya está disponible!"
            }'
